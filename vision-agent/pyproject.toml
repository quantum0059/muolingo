[project]
name = "vision-agent"
version = "0.1.0"
description = "Voice-only AI language teacher for Duolingo audio lessons"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.2",
    "vision-agents[gemini,getstream]>=0.6.3",
]
